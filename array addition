#include<stdio.h>
int main(){
	int a[10],b[10],s[10],i,n;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	for(i=0;i<n;i++);
	scanf("%d",&b[i]);
	for(i=0;i<n;i++)
	s[i]=a[i]+b[i];
	for(i=0;i<n;i++)
	printf("%d\n ",s[i]);
	return 0;	
}
